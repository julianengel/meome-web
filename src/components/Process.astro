---
const steps = [
  {
    number: "01",
    title: "Identify",
    description: "We look for market gaps and opportunities where technology and innovation can make a significant impact.",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
    </svg>`
  },
  {
    number: "02",
    title: "Validate",
    description: "Through market research and MVP testing, we validate our assumptions and refine our approach.",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>`
  },
  {
    number: "03",
    title: "Build",
    description: "We assemble dedicated teams and provide resources to bring these ideas to life.",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
    </svg>`
  },
  {
    number: "04",
    title: "Scale",
    description: "Once product-market fit is achieved, we focus on sustainable growth and market expansion.",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
    </svg>`
  }
];
---

<section id="process" class="py-24 bg-gray-50 dark:bg-gray-800/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16 fade-in">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 dark:text-white sm:text-4xl">
        Our Process
      </h2>
      <p class="mt-4 text-lg text-gray-600 dark:text-gray-300">
        A systematic approach to building successful companies
      </p>
    </div>

    <div class="relative">
      <!-- Connection line -->
      <div class="hidden md:block absolute left-1/2 top-0 bottom-0 w-0.5 bg-gray-200 dark:bg-gray-700"></div>

      <div class="space-y-16">
        {steps.map((step, index) => (
          <div class={`relative fade-in ${index % 2 === 0 ? 'md:text-right' : ''}`}>
            <!-- Timeline dot -->
            <div class="hidden md:block absolute top-8 left-1/2 -ml-1">
              <div class="h-2 w-2 rounded-full bg-indigo-600 dark:bg-indigo-400 ring-4 ring-white dark:ring-gray-900"></div>
            </div>

            <div class={`md:grid md:grid-cols-2 md:gap-8 ${index % 2 === 0 ? '' : 'md:flex-row-reverse'}`}>
              <div class={`md:col-start-${index % 2 === 0 ? '1' : '2'}`}>
                <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm p-6 transform hover:-translate-y-1 transition-transform duration-300">
                  <div class="flex items-center justify-center md:justify-start space-x-4 mb-4">
                    <span class="flex items-center justify-center h-12 w-12 rounded-xl bg-indigo-50 dark:bg-indigo-900/30 text-indigo-600 dark:text-indigo-400">
                      <Fragment set:html={step.icon} />
                    </span>
                    <div>
                      <span class="text-sm font-semibold text-indigo-600 dark:text-indigo-400">Step {step.number}</span>
                      <h3 class="text-xl font-bold text-gray-900 dark:text-white">{step.title}</h3>
                    </div>
                  </div>
                  <p class="text-gray-600 dark:text-gray-300">{step.description}</p>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>